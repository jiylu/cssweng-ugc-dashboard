export interface Deliverable {
  id: number;
  dbId?: string;
  description: string;
  deliverableType: string;
  draftDeadline: string;
  pricing: string;
  quantity: string;
  platform: string;
  contentType: string;
  postDate: string;
}