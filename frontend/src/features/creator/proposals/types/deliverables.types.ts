export interface Deliverable {
  id: number;
  description: string;
  deliverableType: string;
  draftDeadline: string;
  pricing: string;
  quantity: string;
  platform: string;
  contentType: string;
  postDate: string;
}