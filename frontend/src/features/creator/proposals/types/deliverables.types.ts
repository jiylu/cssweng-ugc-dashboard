export interface Deliverable {
  id: number;
  deliverableTitle: string;
  description: string;
  deliverableType: string;
  draftDeadline: string;
  pricing: string;
  quantity: string;
  contentType: string;
  postDate: string;
}